import 'package:flutter/material.dart';

class StudentHomeViewNonMobile extends StatelessWidget {
  const StudentHomeViewNonMobile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // TODO
    );
  }
}