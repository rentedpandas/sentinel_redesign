import 'package:flutter/material.dart';

class StudentHomeViewMobile extends StatelessWidget {
  const StudentHomeViewMobile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // TODO
    );
  }
}