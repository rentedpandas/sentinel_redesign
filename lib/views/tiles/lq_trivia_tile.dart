import 'package:flutter/material.dart';

class LQTriviaView extends StatelessWidget {
  const LQTriviaView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('LQ Trivia'),
    );
  }
}