import 'package:flutter/material.dart';

class FaceVoiceEditorView extends StatelessWidget {
  const FaceVoiceEditorView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Face/Voice Editor'),
    );
  }
}