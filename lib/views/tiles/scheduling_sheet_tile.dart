import 'package:flutter/material.dart';

class SchedulingSheetView extends StatelessWidget {
  const SchedulingSheetView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Scheduling Sheet'),
    );
  }
}