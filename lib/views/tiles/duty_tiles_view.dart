import 'package:flutter/material.dart';

class DutyTilesView extends StatelessWidget {
  const DutyTilesView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tiles'),
    );
  }
}