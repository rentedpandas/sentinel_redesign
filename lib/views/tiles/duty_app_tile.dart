import 'package:flutter/material.dart';

class DutyAppView extends StatelessWidget {
  const DutyAppView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Duty App'),
    );
  }
}