import 'package:flutter/material.dart';

class TFHPassView extends StatelessWidget {
  const TFHPassView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('24 Hour Pass'),
    );
  }
}