import 'package:flutter/material.dart';

class StudentTilesView extends StatelessWidget {
  const StudentTilesView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tiles'),
    );
  }
}