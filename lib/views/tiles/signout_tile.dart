import 'package:flutter/material.dart';

class SignoutAppView extends StatelessWidget {
  const SignoutAppView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Signout App'),
    );
  }
}