import 'package:flutter/material.dart';

class ScreenInteractView extends StatelessWidget {
  const ScreenInteractView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Screen Interact'),
    );
  }
}