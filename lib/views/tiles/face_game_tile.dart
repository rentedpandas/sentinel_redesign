import 'package:flutter/material.dart';

class FaceGameView extends StatelessWidget {
  const FaceGameView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Face Game'),
    );
  }
}