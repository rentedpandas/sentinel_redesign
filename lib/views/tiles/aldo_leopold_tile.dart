import 'package:flutter/material.dart';

class AldoLeopoldView extends StatelessWidget {
  const AldoLeopoldView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Aldo Leopold Map'),
    );
  }
}